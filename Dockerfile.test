# Dockerfile.test
# Minimal Docker image for testing deployment without dependencies

FROM python:3.11-slim

ENV PYTHONUNBUFFERED=1 
ENV PORT=8787

WORKDIR /app

# Install minimal requirements
RUN pip install fastapi uvicorn

# Copy just the test app
COPY test_app.py .

EXPOSE 8787

CMD ["python", "test_app.py"]