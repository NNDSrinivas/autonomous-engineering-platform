[pytest]
pythonpath = .
asyncio_mode = auto
testpaths =
    tests
    backend
    .
norecursedirs =
    .git
    .venv
    aep-venv
    node_modules
    dist
    build
markers =
    integration: tests requiring running services (opt-in via RUN_INTEGRATION_TESTS=1)
