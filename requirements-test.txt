# Test-only dependencies
# Install with: pip install -r requirements-test.txt

# Testing framework
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-timeout>=2.2.0

# Fake Redis for circuit breaker tests (requires lupa for Lua script support)
fakeredis==2.21.1
lupa==2.0

# Code quality
ruff>=0.1.0
black>=23.0.0
