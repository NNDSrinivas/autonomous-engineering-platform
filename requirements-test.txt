# Test-only dependencies
# Install with: pip install -r requirements-test.txt

# Testing framework
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-timeout>=2.2.0

# Fake Redis for circuit breaker tests (requires lupa for Lua script support)
fakeredis>=2.21.1,<3.0.0  # Allow bug fixes while preventing breaking changes
lupa>=2.2  # Use recent version for security and compatibility

# Code quality
ruff>=0.1.0
black>=23.0.0
