// Test file for NAVI generative repair
function brokenFunction() {
    const message = "Hello world";
    console.log(message
  // Missing closing parenthesis and brace

function anotherFunction() {
            if (true {
                return "test"
// Missing closing parenthesis and brace

// Missing closing brace for file scope